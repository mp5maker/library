function setup() {
    createCanvas(500, 500);

    /* For Point */
    strokeWeight(10);
}

function draw() {
    /* Clear Rect */
    // background(255);

    /* Point */
    point(0, 0)
    point(100, 0)
    point(100, 100)
    point(0, 100)
}
const assert = require('assert')

describe('Simple Math Test', () => {
    it('Should Return 2', () => {
        assert.equal(1 + 1, 2)
    })
    it('Should Return 9', () => {
        assert.equal(3 * 3, 9)
    })
})
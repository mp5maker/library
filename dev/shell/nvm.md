```bash
nvm install 8.10.0 # specific version install
nvm use 8.10.0 # temporary change node to specific version
nvm install node --lts # stable version install
nvm alias default 8.10.0 # permenantly change to specific version
```